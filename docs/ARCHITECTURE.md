# Architecture

Phase 1 provides the base workspace structure for a future split between the Next.js app and the WordPress plugin.

The app shell lives in `apps/web`, the config templates live in `config/`, and operational scripts live in `scripts/`.
