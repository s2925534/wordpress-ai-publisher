# Architecture

Phase 1 provides the base workspace structure for a future split between the Next.js app and the WordPress plugin.

The app shell lives in `apps/web`, the config templates live in `config/`, the WordPress plugin scaffold lives in `wordpress-plugin/publisher-plugin`, the discovery service and cached snapshot flow live in `apps/web/server/discovery-service.ts`, and operational scripts live in `scripts/`.
